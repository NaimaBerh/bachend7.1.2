# =============================================================================
#  Dépendances Python pour le backend FakeGuard / FPD v7
#  Version optimisée pour l'hébergement Render (slug < 500 Mo)
# =============================================================================
#  Installation locale :
#    pip install -r requirements.txt
# =============================================================================

# --- Web framework ---
Flask==3.0.3
flask-cors==4.0.1
gunicorn==22.0.0

# --- ML : Random Forest + TF-IDF + scaler ---
scikit-learn==1.5.2
joblib==1.4.2
numpy==1.26.4
scipy==1.13.1

# --- LSTM (inférence TFLite uniquement) ---
# tflite-runtime remplace tensorflow (~500 Mo → ~3 Mo).
# Le tokenizer Keras est désormais réimplémenté en pure Python dans app.py,
# donc tensorflow N'EST PLUS nécessaire pour le runtime.
tflite-runtime==2.14.0

# --- Extraction d'URL ---
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.2.2
